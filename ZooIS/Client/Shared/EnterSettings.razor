@inject NavigationManager _navigationManager

<AuthorizeView>
    <MudButton Variant="Variant.Text" Color="Color.Inherit">Nustatymai</MudButton>
</AuthorizeView>

@code {
    void ToSettings()
    {
        _navigationManager.NavigateTo("/settings");
    }
}