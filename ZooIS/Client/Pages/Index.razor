@page "/"
@using MudBlazor
@using ZooIS.Client.Services.UserSettingsService;
@inject IUserSettingsService SettingsService

<h3>Main Screen...</h3>

@*
@if (loaded)
{
    @Settings.Id
    @Settings.Language
    @Settings.DarkMode
}
*@


@code {
    bool loaded = false;
    UserSettings Settings = null;



    protected override async Task OnInitializedAsync()
    {
        //Settings = await SettingsService.GetSettings(1);
        loaded = true;
    }
}