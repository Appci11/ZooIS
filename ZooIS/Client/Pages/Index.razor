@page "/"
@using ZooIS.Client.Services.ZooMapService
@inject IZooMapService _zooMapService

@if (loaded)
{
    <MudButton OnClick="Kriu">Kriu</MudButton>
}

@code {
    bool loaded = true;


    async void Kriu()
    {
        await _zooMapService.GetAreasColorsByTags();
    }
}